require - функция которая принимает путь до файла ввиде строки, есть два вида путей: 1) global(require('os')), 2) local(require("./greet")); результат поиска модуля можно записать в переменную: const greet = require("./greet")

